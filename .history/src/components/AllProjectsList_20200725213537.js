import React from 'react';
import AllProject

export default function AllProjectsList({allProjects}) {
    console.log(allProjects);
    
    return (
        <div>
            {allProjects.length}
        </div>
    )
}
