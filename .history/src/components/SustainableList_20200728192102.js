import React from 'react';
import SustainableList from './SustainableItem';


export default function SustainableList({ projects }) {
    return (
        <div>
            {projects.length}
        </div>
    )
}
