import React from 'react'


export default function QualityDetails() {
    
    let { qualityModal, setQualityModal } = const context = useContext(contextValue)

    return (
        <div>
            
        </div>
    )
}
