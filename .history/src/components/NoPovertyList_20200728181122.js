import React from 'react';
import NoPovertyItem from './NoPovertyItem';

export default function NoPovertyList({projects}) {
    
    const     projects.map(project => (
            <NoPovertyItem  />
        ))

    return (
        <div>
            
            {/* <NoPovertyItem project={projects} />
            {projects.length} */}
        </div>
    )
}
