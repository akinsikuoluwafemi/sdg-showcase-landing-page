import React from 'react';
import SustainableList from './Sus'

export default function SustainableList({ projects }) {
    return (
        <div>
            {projects.length}
        </div>
    )
}
